name: zodgame

on:
  schedule:
    - cron: "0 0 * * *"
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Install requirements
      run: |
        pip3 install -r ./zodgame/requirements.txt
        
    - name: Run
      run: |
        python3 ./zodgame/zodgame.py "${{secrets.ZODGAME_COOKIE}}"

    # --- ✅ Success Notification (PowerShell) ---
    - name: Send Gotify Notification on Success
      if: success()
      shell: pwsh
      run: |
        $params = @{
            title    = "ZodGame 签到成功"
            message  = "每日签到任务已成功完成。"
            priority = 5
        }
        $url = "${{ secrets.GOTIFY_URL }}/message?token=${{ secrets.GOTIFY_TOKEN }}"
        Invoke-RestMethod -Uri $url -Method Post -Body $params

    # --- 🚨 Failure Notification (PowerShell) ---
    - name: Send Gotify Notification on Failure
      if: failure()
      shell: pwsh
      run: |
        $params = @{
            title    = "ZodGame 签到失败"
            message  = "脚本执行出错，请检查 GitHub Actions 日志。"
            priority = 8
        }
        $url = "${{ secrets.GOTIFY_URL }}/message?token=${{ secrets.GOTIFY_TOKEN }}"
        Invoke-RestMethod -Uri $url -Method Post -Body $params

    - uses: liskin/gh-workflow-keepalive@main
